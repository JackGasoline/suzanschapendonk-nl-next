const loadingState = {
    opacity: 0,
    transition: {
      duration: 0,
    },
  };
  
  const exitState = {
    y: "100vh",
    opacity: 0,
    transition: {
      duration: 1,
    },
  };

  export {loadingState, exitState};